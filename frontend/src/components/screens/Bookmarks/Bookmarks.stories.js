import Bookmarks from './Bookmarks';

/** The **Bookmarks** component renders the Bookmarks page. */
export default {
  title: 'screens/screens/Bookmarks',
  component: Bookmarks,
  tags: ['autodocs'],
};

/** This is the default version of the **Bookmarks** component:*/
export const Default = {
  args: {
  },
};