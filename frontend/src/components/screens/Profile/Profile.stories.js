import Profile from './Profile';

/** The **Profile** component renders the Profile page. */
export default {
  title: 'screens/screens/Profile',
  component: Profile,
  tags: ['autodocs'],
};

/** This is the default version of the **Profile** component:*/
export const Default = {
  args: {
  },
};